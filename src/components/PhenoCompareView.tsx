import React from 'react';

interface PhenoCompareViewProps {}

const PhenoCompareView: React.FC<PhenoCompareViewProps> = (props) => {
  return <h1>Pheno Compare view</h1>;
};

export default PhenoCompareView;
export const url = '/pheno_compare';
