import React from 'react';

interface PhenoViewProps {}

const PhenoView: React.FC<PhenoViewProps> = (props) => {
  return <h1>Pheno View</h1>;
};

export default PhenoView;
export const url = '/pheno';
