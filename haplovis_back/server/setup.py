from setuptools import setup, find_packages

setup(
    name='server',
    version='0.1',
    description='Server that holds functionality for haplotype visualization.',
    author='Ethan Keller',
    packages=find_packages(),
    # install_requires=[
    #     'markdown',
    # ],
)