from setuptools import setup, find_packages

setup(
    name='cli',
    version='0.1',
    description='CLI that offers commands to generate layouts for GFA files.',
    author='Ethan Keller',
    packages=find_packages(),
    # install_requires=[
    #     'markdown',
    # ],
)